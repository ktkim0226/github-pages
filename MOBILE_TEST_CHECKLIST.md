# v1.0.26 모바일 점검표

1. GitHub Pages HTTPS 주소로 접속하고 카메라 권한 허용
2. 상태에 `다중 프레임 검증 활성` 표시 확인
3. 같은 바코드에서 후보 1/2 → 2/2 후 확정 확인
4. 카메라 선택 목록 변경 후 영상 재시작 확인
5. Galaxy S21/S22/S23 Ultra/S26+와 POCO X6 Pro에서 자동 점검 후 중앙 영상이 가장 선명한 후면 렌즈로 시작하는지 확인
6. 카메라 시작 직후 중앙 코드에 자동으로 초점이 맞고 연속 초점 상태가 유지되는지 확인
7. 흐린 상태가 계속되면 자동 재초점되고, 선명해지면 초점 헌팅이 중단되는지 확인
8. 초점 버튼 선택 시 중앙 재초점 후 연속 초점으로 복귀하는지 확인
9. 카메라 영상의 코드 위치를 터치했을 때 노란 초점 표시가 나타나고 해당 위치가 선명해지는지 확인
10. 터치 좌표 초점 미지원 기기에서도 화면 터치 후 중앙 자동초점과 스캔이 계속 동작하는지 확인
11. `카메라 조명`, `자동 초점` 문구가 이전보다 약 2배 크게 보이는지 확인
12. 1×/2×/3× 줌 및 슬라이더 확인
13. 어두움·과다 반사·흐림 안내 확인
14. QR, Code128, EAN-13, DataMatrix 실물 확인
15. 스캔 프레임 밖에 다른 바코드를 놓고 프레임 내부 코드만 인식되는지 확인
16. 프레임 안에서 작게 보이는 1D 바코드와 QR 코드가 인식되는지 확인
17. XLSX 설치국사·셀프 바코드·모듈 헤더 확인
18. 신규 증설에서 카테고리와 유니트를 선택하지 않고 유니트명을 직접 입력해 저장되는지 확인
19. 유니트 목록 선택과 직접 입력을 함께 사용하면 직접 입력값이 저장되는지 확인
20. 재배치에서 카테고리와 유니트를 선택하지 않고 유니트명을 직접 입력해 저장되는지 확인
21. 재배치 유니트 목록 선택과 직접 입력을 함께 사용하면 직접 입력값이 저장되는지 확인
22. 재배치 저장 후 수동 유니트명은 유지되고 Slot과 유니트바코드만 비워지는지 확인
23. `입력값 초기화` 선택 시 재배치 수동 유니트명까지 삭제되는지 확인
24. 재배치 전 정보만 저장한 건에 `재배치 전 저장` 상태가 표시되는지 확인
25. 저장 목록의 `불러오기` 선택 시 해당 데이터가 `2. 재배치 정보 입력`란에 채워지는지 확인
26. 재배치 후 정보를 입력하고 수정 저장할 때 기존 행이 갱신되는지 확인
27. 재배치 후 정보가 있는 건에 `재배치 완료` 상태가 표시되는지 확인
28. 수정 취소 시 편집 상태와 입력값이 정상 초기화되는지 확인
29. 앱에서 내보낸 재배치 XLSX 한 건을 불러오면 `2. 재배치 정보 입력`란에 바로 표시되는지 확인
30. 여러 건의 재배치 XLSX를 불러오면 목록에서 각 건을 선택할 수 있는지 확인
31. 같은 XLSX를 다시 불러올 때 중복 데이터가 추가되지 않는지 확인
32. 신규 증설 저장 목록에 스캔한 유니트 바코드가 표시되는지 확인
33. 이전 버전에서 저장한 신규 증설 데이터도 Slot 바코드가 유니트 바코드 열에 표시되는지 확인
34. 재배치 저장 목록에 유니트 바코드가 표시되는지 확인
35. Android Chrome에서 공유 버튼 문구가 `내용 복사 + XLSX 저장`으로 표시되는지 확인
36. Android Chrome에서 버튼 한 번으로 내용이 복사되고 XLSX가 저장되는지 확인
37. Android Chrome에서 `NotAllowedError` 등 공유 API 오류가 표시되지 않는지 확인
38. 재배치 저장 후 재배치 전·후 국사와 링 정보가 유지되는지 확인
39. 재배치 저장 후 재배치 전·후 Slot과 유니트바코드만 비워지는지 확인
40. 유지된 국사와 링 정보를 사용자가 직접 수정할 수 있는지 확인
41. `입력값 초기화` 선택 시 유지된 값을 포함해 재배치 입력 전체가 초기화되는지 확인
42. iPhone에서 공유 버튼 문구가 `카카오톡 파일+내용 공유`로 표시되는지 확인
43. iPhone 공유 메뉴에서 카카오톡을 선택하고 XLSX 파일과 저장 내용이 함께 전달되는지 확인
44. 결합 공유 미지원 또는 거부 환경에서 내용 복사와 XLSX 저장으로 자동 전환되는지 확인
45. Android에서 저장된 XLSX와 복사된 내용을 각각 정상적으로 사용할 수 있는지 확인
46. iPhone에서 기존처럼 XLSX 파일과 저장 내용이 한 번에 공유되는지 확인
47. Android Chrome에서 같은 버튼을 반복해도 공유 API 오류가 발생하지 않는지 확인
48. 사용자가 iPhone 공유창을 닫았을 때 XLSX가 중복 저장되지 않는지 확인
49. Android 복사 실패 환경에서도 XLSX 저장은 정상 수행되는지 확인
